{
  "name": "crud-pevn-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve:backend": "cd backend && npm install && npx prisma migrate reset --force && npm run dev",
    "serve:frontend": "cd frontend && npm install && npm run dev",
    "prod:backend:build": "cd backend && npm install && npx prisma migrate reset --force",
    "prod:backend:dev": "cd backend && npm run dev",
    "prod:frontend:build": "cd frontend && npm install",
    "prod:frontend:dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}