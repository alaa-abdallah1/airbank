{
  "name": "crud-pevn-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve:backend": "cd backend && npm install && npx prisma migrate reset --force && npm run dev",
    "serve:frontend": "cd frontend && npm install && npm run dev",
    "prod:build": "cd backend && npm install && npx prisma migrate reset --force",
    "prod:dev": "npm run dev",
    "prod:frontend": "yarn serve:frontend"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}